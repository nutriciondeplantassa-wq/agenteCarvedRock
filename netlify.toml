# Netlify configuration for Next.js
[build]
  command = "npm run build"

# Required: Netlify will automatically install this plugin for Next.js sites
# If not installed, manually add it in Netlify UI under "Plugins"
